解决了数据源tab关闭后重新打开时数据不重新加载的问题，方式是每次进入tab时检查一下是否是新创建的（即不是在tab栏中已打开的），是新创建的的话就更新一些表格数据

FIXME 主页面的组件或许不应该keep-alive，否则对数据库的修改不会更新到组件，如新增了一个数据源。或者在生命周期中加一些请求。
FIXME 项目的结构需要调整一下
FIXME 字体图标目前是引用在线链接，完成项目后需改为本地的
FIXME 调整一下尤其是vue文件中各种方法的顺序
FIXME 对路径进行判断，比如直接通过地址栏访问一个不存在的数据源，就怎么怎么样
FIXME 把css和js看情况抽离出来，一些不需要数据库但可以整理成json的就通过json导入
FIXME 把api中的log改成其他的样式
FIXME 有些props其实是不需要的，获取父组件内容可以用this.$parent，获取子组件内容可以通过获取子组件的DOM

NOTE package 里的 identify 是做图形验证码的，可以在掘金搜索

FIXME 由于表格数据都会转化成String，所以传给chart后会有点不对劲，可以尝试加一些转化等
FIXME 页面的自适应还是有问题，尤其是在开启开发者工具的情况下，有时chart不resize，有时table点击时滚动条异常
FIXME 不同浏览器有不同的默认样式，如字体、字体大小等（如火狐），需要用不同的浏览器测试。css也需要做兼容。